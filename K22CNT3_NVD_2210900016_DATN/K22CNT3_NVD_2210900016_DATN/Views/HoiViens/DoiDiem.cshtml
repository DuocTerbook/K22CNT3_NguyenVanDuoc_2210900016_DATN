@model K22CNT3_NVD_2210900016_DATN.Models.HoiVien

@{
    ViewBag.Title = "Doi diem";
}

<h3 class="text-danger">
    <i class="fa-solid fa-minus"></i> Doi diem hoi vien
</h3>

<div class="card shadow-sm mt-3">
    <div class="card-body">

        <p><b>Ma the:</b> @Model.MaThe</p>
        <p><b>Khach hang:</b> @Model.KhachHang.TenKhach</p>
        <p>
            <b>Diem hien tai:</b>
            <span class="fw-bold text-success">@Model.DiemTichLuy</span>
        </p>

        @using (Html.BeginForm("DoiDiem", "HoiViens", FormMethod.Post))
        {
            @Html.AntiForgeryToken()
            @Html.Hidden("id", Model.ID_HoiVien)

            <!-- BẮT BUỘC name="diem" -->
            <div class="mb-3">
                <label class="form-label">So diem can doi</label>
                <input type="number"
                       name="diem"
                       class="form-control"
                       min="1"
                       max="@Model.DiemTichLuy"
                       required />
            </div>

            <div class="mb-3">
                <label class="form-label">Ly do</label>
                <input type="text"
                       name="lyDo"
                       class="form-control" />
            </div>

            <button type="submit" class="btn btn-danger">
                <i class="fa-solid fa-check"></i> Xác nhận
            </button>

            <a href="@Url.Action("Details", new { id = Model.ID_HoiVien })"
               class="btn btn-secondary ms-2">
                Hủy
            </a>
        }
    </div>
</div>
